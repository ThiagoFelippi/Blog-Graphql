type Post{
  title: String
  content: String
}

input PostInput{
  title: String!, 
  content: String!
}

type Query{
  posts: [Post!]!
  post(title: String!): Post
}

type Mutation{
  createPost(data: PostInput) : Post!
  updatePost(data: PostInput) : Post!
}