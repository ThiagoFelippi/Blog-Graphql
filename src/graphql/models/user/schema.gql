type User{
  name: String
  email: String
  password: String
}

input UserInput{
  title: String!, 
  content: String!
}

type Query{
  users: [User!]!
  user(email: String!): User
}

type Mutation{
  createUser(data: UserInput) : User!
  updateUser(data: UserInput) : User!
}